<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.*?>
<?import java.lang.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.image.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.shape.SVGPath?>
<?import javafx.geometry.Insets?>

<VBox alignment="CENTER" spacing="20.0" stylesheets="@register-view.css" xmlns="http://javafx.com/javafx/8" xmlns:fx="http://javafx.com/fxml/1" fx:controller="org.example.bookrecommender2.EventHandler">
    <padding>
        <Insets bottom="50.0" left="50.0" right="50.0" top="50.0" />
    </padding>
    <children>

        <!-- Logo e titolo -->
        <!-- Modifica la sezione Logo e titolo -->
        <HBox alignment="CENTER" spacing="20.0">
            <VBox alignment="CENTER_LEFT" spacing="10.0">
                <Label styleClass="title-label" text="BookRecommender" />
                <Label styleClass="subtitle-label" text="Crea il tuo account" />
            </VBox>
            <ImageView fitHeight="170.0" fitWidth="170.0" preserveRatio="true">
                <image>
                    <Image url="@/libraryicon2.png" />
                </image>
                <HBox.margin>
                    <Insets top="-20.0" />
                </HBox.margin>
            </ImageView>
            <VBox.margin>
                <Insets bottom="10.0" />
            </VBox.margin>
        </HBox>

        <!-- Campi nome e cognome affiancati -->
        <HBox alignment="CENTER" maxWidth="Infinity" spacing="10" styleClass="responsive-container">
            <children>
                <VBox spacing="5" HBox.hgrow="ALWAYS">
                    <children>
                        <Label styleClass="input-label" text="Nome" />
                        <TextField fx:id="firstNameField" styleClass="input-field" />
                    </children>
                </VBox>
                <VBox spacing="5" HBox.hgrow="ALWAYS">
                    <children>
                        <Label styleClass="input-label" text="Cognome" />
                        <TextField fx:id="lastNameField" styleClass="input-field" />
                    </children>
                </VBox>
            </children>
        </HBox>

        <!-- Codice Fiscale -->
        <VBox maxWidth="Infinity" spacing="5" styleClass="responsive-container">
            <children>
                <Label styleClass="input-label" text="Codice Fiscale" />
                <TextField fx:id="taxCodeField" styleClass="input-field" />
            </children>
        </VBox>

        <!-- Email -->
        <VBox maxWidth="Infinity" spacing="5" styleClass="responsive-container">
            <children>
                <Label styleClass="input-label" text="Email" />
                <TextField fx:id="emailField" styleClass="input-field" />
            </children>
        </VBox>

        <!-- Password -->
        <!-- Password -->
        <VBox maxWidth="Infinity" spacing="5" styleClass="responsive-container">
            <children>
                <Label styleClass="input-label" text="Password" />
                <HBox alignment="CENTER_LEFT" styleClass="password-container">
                    <children>
                        <PasswordField fx:id="passwordField" styleClass="input-field" HBox.hgrow="ALWAYS" />
                    </children>
                </HBox>
            </children>
        </VBox>
        <!-- Pulsanti -->
        <HBox alignment="CENTER" maxWidth="Infinity" spacing="10" styleClass="button-container">
            <children>
                <Button fx:id="homeButton" onAction="#switchToHome" styleClass="secondary-button" text="Home" />
                <Button fx:id="LoginButton" onAction="#switchToLogin" styleClass="secondary-button" text="Ho già un account" />
                <Button fx:id="registerButton" onAction="#registerUser" styleClass="primary-button" text="Registrati" />
            </children>
        </HBox>
    </children>
</VBox>
